## Info

This is a simple node module used to convert all files found in a folder into a single folder. Useful for cases were you may need to keep the files small for storage but need to act on the files as a single object.